interface:
display_name: "Verify Agent Tasks with Kona"
short_description: "Run local Agent tasks against explicit acceptance checks"
default_prompt: "Use $kona-capture to define, validate, run, and inspect a local Agent task contract before handoff."
